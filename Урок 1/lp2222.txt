+1. Enum - написать свой класс и enum к нему.


 

2. Передача параметров по ссылке и значению. Выходные параметры (ref out) - Выучить разницу между ссылочным и значимым методом. Их разница при передаче в метод. Написать примеры методов где передаются и изменяются ссылочные и значимые типы + примеры с ref и out.
		(Если передаем в метод ссылочный тип данных без реф, то можно менять данные но не ссылку, т.к.
		 сама ссылка это значимый тип)
		Разница между ref и out - out обязывает присваивать значение параметра(с out) внутри метода до выхода с него  

3. Структуры - выучить про разницу между классом и структурой. Написать свою структуру.

	Структуру лучше использовать когда не нужны наследование и полиморфизм и занимает мало места в оперативной памяти
	Структуру лучше не использовать КОГДА в ней много полей и она передается в качестве параметра метода.(модификаторы 
		ref in могут ускорить структуру)

	Структура позволяет избежать частой работы сборщика мусора

""""""""""""""""""""ПОДТВЕРДИТЬ ОТВЕТЫ, ЗАПИСАТЬ В ЧИТШИТ